package org.example.streamex;

public class StreamExample {
    /*
     * 데이터의 흐름
     * 컬렌션 형태로 구성된 데이터를 람다를 이용해 간결하고 직관적으로 프로세스하게 해줌
     * For, While 등을 이용하던 기존 loop을 대체
     * 손쉽게 병렬 처리를 할 수 있게 해줌
     *
     * 구조
     * 1. Source : Stream을 생성하는 소스 (컬랙션, 배열 등)
     * 2. Intermediate Operation : Stream을 변환하는 중간 연산 (filter, map, sorted 등)
     * 3. Terminal Operation : Stream을 소모하는 최종 연산 (forEach, collect, reduce 등)
     *
     * */
}
